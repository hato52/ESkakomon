<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hato.eskakomon">

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@drawable/es_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/es_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Sample AdMob App ID: ca-app-pub-3940256099942544~3347511713 -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-1227823838804298~2597786484"/>

        <activity android:name=".CheckedQuestionActivity" />
        <activity android:name=".MissedQuestionActivity" />
        <activity android:name=".QuestionViewActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".QuestionActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/NoActionBarTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>